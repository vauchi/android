# Local configuration (machine-specific)
local.properties

# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml

# Native libraries (generated by build script)
app/src/main/jniLibs/

# NOTE: UniFFI bindings are now tracked in git (matching iOS policy).
# This prevents drift between platforms and ensures builds work after clone.
# To regenerate: cd ../code && ./scripts/build-bindings.sh --android
# See docs/planning/done/POSTMORTEM-ANDROID-BINDINGS-DRIFT.md for rationale.
